version: "2.3"

services:
  playground:
    build:
      context: ./
      network: host
    ports:
      - "5900:5900"
      - "6000:6000"
    stdin_open: true
    tty: true
    volumes:
      - ./scripts:/scripts
      - ./demos:/demos
      - ./:/repo   
    environment:
      - VERSION=0.1
    entrypoint: ["/bin/bash"]